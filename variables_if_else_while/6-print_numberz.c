#include <stdio.h>
/**
 * main - Loop prints all single digit numbers of base 10 without char
 *
 * Return: the digit numbers of base 10
 */
int main(void)
{
	int i;

	for (i = 48; i <= 57; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
