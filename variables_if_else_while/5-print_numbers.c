#include <stdio.h>
/**
 * main - Loop for print all single digit numbers
 *
 * Return: list from 0 to 9 
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
