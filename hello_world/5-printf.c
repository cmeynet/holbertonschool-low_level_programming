#include <stdio.h>
/**
 * main -  Prints "with proper grammar, but the outcome is a piece of art,"
 *
 *  Return: the prints of the sentence
 */
int main(void)
{
        printf("%s", "with proper grammar, but the outcome is a piece of art,\n");
        return (0);
}
